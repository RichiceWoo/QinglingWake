# 单测 Spec — 需求名称

> status: propose | apply | done  
> created: YYYY-MM-DD  

## 0. 测试原则

- **展示证据**：运行测试后摘录关键输出；避免无依据的「已通过」。
- **与 tasks 对齐**：每个 P0 场景尽量对应 `tasks.md` 中可验收条目。

## 1. 测试框架

| 项目 | 值 |
|------|-----|
| JUnit | 5 |
| Mock | Mockito（与项目一致） |
| 模块 | 例如 `benefit-app`、`benefit-domain`（按本需求实际填写） |

## 2. 覆盖范围

### P0 — 必须通过

| 类/组件 | 方法或场景 | 输入 | Mock/前置 | 预期 |
|---------|------------|------|-------------|------|

### P1 — 建议

### P2 — 可选

### 不测试（写明原因）

## 3. 执行计划

- [ ] 运行现有相关模块测试，确认基线
- [ ] 新增/修改测试 → 红绿与回归
- [ ] 在 `spec.md` 或本文件记录未覆盖风险（若有）

## 4. 常用命令（按需调整）

```bash
cd benefit-center && mvn -pl benefit-starter -am test
```
